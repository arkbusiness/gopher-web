export interface HeroSlide {
  id: string;
  image: string;
  alt: string;
  title: string;
}

export interface HeroContent {
  title: string;
  description: string;
  ctaText: string;
  ctaLink: string;
}
